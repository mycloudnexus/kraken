app:
  mgmt:
    token-storage:
      enabled: true
      engine: vault
    vault:
      namespace: secret/data
spring:
  cloud:
    vault:
      uri: http://localhost:8200
      authentication: TOKEN
      token: ${VAULT_ACCESS_TOKEN:hvs.Rn201wqJfr47TlxbaNtq48L9}
      enabled: true
      reactive:
        enabled: false